# PostgreSQL connection string used by Prisma (prisma7.config.ts / src/lib/prisma.ts)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/tournament?schema=public"

# Better Auth
# BETTER_AUTH_SECRET は `openssl rand -base64 32` などで生成する
BETTER_AUTH_SECRET="replace-with-a-random-32-byte-string"
# BETTER_AUTH_URL は実際にアプリを配信しているオリジンと一致させること。
# 3000番ポートが使用中で next dev が別ポートにフォールバックした場合も、
# 実際に開いているポートに合わせて変更する必要がある。
# ずれていると、Cookie を伴わない curl 等の疎通確認は通ってしまう一方、
# ログイン後のブラウザからのリクエスト（ログアウトなど）だけが
# 403 INVALID_ORIGIN で失敗するため気づきにくい。
BETTER_AUTH_URL="http://localhost:3000"

# Google OAuth（Google Cloud Console で発行する。手順は README を参照）
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# 開発用の認証バイパス。"1" のときだけ有効。
# USER_ID Cookie に User.id を入れるだけで、そのユーザーとしてログイン状態になる。
# 署名検証もパスワード確認も一切行わない完全な裏口なので、
# 本番環境では絶対に設定しないこと（未設定なら通常のログインのみ動く）。
# BYPASS_AUTH="1"

# Mailtrap（メール配信）
# MAILTRAP_TOKEN が空の場合は送信せず、確認 URL をサーバログに出力する。
# ローカルではこのままでも登録フローを最後まで踏める。
# ただし NODE_ENV=production かつ空だと、メール送信時に例外になる。
MAILTRAP_TOKEN=""
# "1" のとき Mailtrap の Email Testing（sandbox）受信箱へ送る。
# 実際のメールアドレスには届かないので、開発中はこちらを使う。
# その場合 MAILTRAP_TEST_INBOX_ID も必須。
MAILTRAP_SANDBOX="1"
MAILTRAP_TEST_INBOX_ID=""

# 確認メールの差出人。未設定でも既定値で動く（届かない原因としては軽いため）。
MAIL_FROM_ADDRESS="no-reply@example.com"
MAIL_FROM_NAME="大会運営"

# Google Analytics 4 の測定 ID（G- で始まる）。
# 空または未設定のとき、gtag.js の読み込みもイベント送信も一切行わない。
# NEXT_PUBLIC_ 接頭辞のとおりブラウザに露出する値なので、秘密ではない。
# ローカル開発では空のままにし、本番の計測に開発中の操作を混ぜないこと。
NEXT_PUBLIC_GA_ID=""
